# Y1 Helper Application Dependencies (Modern)
# Core dependencies for the application with modern version specifications

# Image processing and analysis
Pillow>=9.0.0
numpy>=1.21.0

# HTTP requests for GitHub API integration
requests>=2.25.0

# Note: tkinter is included with Python standard library
# Note: All other imports (subprocess, threading, time, os, struct, json, 
# platform, ctypes, tempfile, shutil, re) are part of Python standard library 